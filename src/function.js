/**
 *
 * Descripcion general de la función.
 * 
 * @param {number} n1  Este es el primer numero que se requiere para utilizar la funcion.
 * @param {number} n2 Este es un parametro de tipo numerico.
 * @returns {number} Este numero returna un numero que es el resultado de la suma de los dos parametros que se madaron.
 */

function add(n1, n2){
    return n1 + n2;

}

/**
 * Add two numbers and return a string.
 * @param {number} n1 First number
 * @param {number} n2 Second Number
 * @returns {string} Regresa un string
 */
const addtwo = (n1, n2) => `The result is : ${n1 + n2}`;














